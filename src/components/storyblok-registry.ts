// This file exists to avoid the casing conflicts with direct imports
import PageComponent from './storyblok/page';
import PageHomeComponent from './storyblok/page-home/page-home';
import TeaserComponent from './storyblok/teaser';

export const storyblokComponents = {
  page: PageComponent,
  'page-home': PageHomeComponent,
  teaser: TeaserComponent,
};
