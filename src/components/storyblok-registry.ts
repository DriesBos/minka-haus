// This file exists to avoid the casing conflicts with direct imports
import Page from './storyblok/page';
import PageHome from './storyblok/page-home/page-home';
import Teaser from './storyblok/teaser';

export const storyblokComponents = {
  page: Page,
  'page-home': PageHome,
  teaser: Teaser,
};
