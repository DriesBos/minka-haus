// This file exists to avoid the casing conflicts with direct imports
import Page from '@/components/storyblok/page';
import PageHome from '@/components/storyblok/page-home/page-home';
import Teaser from '@/components/storyblok/teaser';

export const storyblokComponents = {
  page: Page,
  'page-home': PageHome,
  teaser: Teaser,
};
